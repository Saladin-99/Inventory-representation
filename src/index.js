import React from "react"
import ReactDOM from "react-dom"
//component file
import InvContainer from  "./components/InvContainer"

//stylesheet
import "./App.css"

ReactDOM.render(
    <React.StrictMode>
    <InvContainer/>
    </React.StrictMode>, document.getElementById("root"))